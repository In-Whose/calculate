{
  "name": "go-stop-money",
  "version": "1.0.0",
  "private": true,
  "_comment_original": "Codex Sites starter package settings were replaced for the requested static GitHub Pages app.",
  "_comment_original_ocr_dependency": "tesseract.js 6.0.1",
  "_comment_original_build_scripts": "build: tsc -b && vite build; build:sites was not present.",
  "_comment_previous_paddle_build_scripts": "Both PaddleOCR build scripts ended immediately after vite build, with prepare-sites-worker added only to build:sites.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && node scripts/prepare-ocr-assets.mjs && vite build && node scripts/remove-unused-ort-assets.mjs",
    "build:sites": "tsc -b && node scripts/prepare-ocr-assets.mjs && vite build && node scripts/remove-unused-ort-assets.mjs && node scripts/prepare-sites-worker.mjs",
    "preview": "vite preview",
    "lint": "eslint src tests --max-warnings=0",
    "typecheck": "tsc -b --pretty false",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "@paddleocr/paddleocr-js": "0.4.2",
    "dexie": "4.0.11",
    "react": "19.2.0",
    "react-dom": "19.2.0",
    "react-router-dom": "7.9.6",
    "zod": "4.1.13"
  },
  "devDependencies": {
    "@eslint/js": "9.39.1",
    "@playwright/test": "1.57.0",
    "@testing-library/jest-dom": "6.9.1",
    "@testing-library/react": "16.3.0",
    "@types/node": "24.10.1",
    "@types/react": "19.2.5",
    "@types/react-dom": "19.2.3",
    "@vitejs/plugin-react": "5.1.1",
    "eslint": "9.39.1",
    "eslint-plugin-react-hooks": "7.0.1",
    "eslint-plugin-react-refresh": "0.4.24",
    "fake-indexeddb": "6.2.4",
    "globals": "16.5.0",
    "jsdom": "27.2.0",
    "typescript": "5.9.3",
    "typescript-eslint": "8.48.0",
    "vite": "7.2.6",
    "vitest": "4.0.15"
  },
  "type": "module"
}
